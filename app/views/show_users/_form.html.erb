<%= form_for(@show_user) do |f| %>
  <% if @show_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show_user.errors.count, "error") %> prohibited this show_user from being saved:</h2>

      <ul>
      <% @show_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :show_id %><br>
    <%= f.text_field :show_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
