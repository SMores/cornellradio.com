<h1>Listing shows</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Profilepic</th>
      <th>Description</th>
      <th>Fbpage</th>
      <th>Twpage</th>
      <th>Extpage</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @shows.each do |show| %>
      <tr>
        <td><%= show.title %></td>
        <td><%= show.profilePic %></td>
        <td><%= show.description %></td>
        <td><%= show.fbPage %></td>
        <td><%= show.twPage %></td>
        <td><%= show.extPage %></td>
        <td><%= link_to 'Show', show %></td>
        <td><%= link_to 'Edit', edit_show_path(show) %></td>
        <td><%= link_to 'Destroy', show, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Show', new_show_path %>
