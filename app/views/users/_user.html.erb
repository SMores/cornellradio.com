<li>
  <%= link_to(user) do %>
    <% if ! user.profile_pic.blank? %>
      <%= image_tag user.profile_pic.url, class: "pic" %>
    <% end %>
    <span class="title"><%= user.first_name+' '+user.last_name %></span>
  <% end %>
</li>
</a>